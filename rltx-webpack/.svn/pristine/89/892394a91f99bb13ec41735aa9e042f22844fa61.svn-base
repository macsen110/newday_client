/**
 * maintest.js
 * Created by dsky on 17/5/17.
 */
import Vue from 'vue';
import { Table, TableColumn, Pagination, Loading, Input, Select, Button, Form, FormItem, Option, row, col, MessageBox, Message } from 'element-ui';
import apptest from './appTest.vue';

Vue.use(Table);
Vue.use(TableColumn);
Vue.use(Pagination);
Vue.use(Loading);
Vue.use(Input);
Vue.use(Select);
Vue.use(Button);
Vue.use(Form);
Vue.use(FormItem);
Vue.use(Option);
Vue.use(row);
Vue.use(col);

Vue.prototype.$msgbox = MessageBox;
Vue.prototype.$message = Message;

Vue.config.productionTip = false;

/* eslint-disable no-new */
/* eslint-disable no-unused-vars */

const vm = new Vue({
  el: '#app',
  template: '<apptest/>',
  components: { apptest },
  computed: {},
  mounted() {
  }
});
